server:
  port: 7082

eureka:
  client:
    enabled: false

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa

movieService: http://localhost:8083