server:
  port: 7085

eureka:
  client:
    enabled: false

# Disable Circuit Breaker (Hystrix)
spring:
  cloud:
    circuit:
      breaker:
        enabled: false

  session:
    store-type: none

hystrix:
  command:
    default:
      circuitBreaker:
        enabled: false